% Notes

\rowcolors{1}{notesColor}{notesColor}
\begin{tabularx}{\textwidth}{X}
  \hline

  \noteItem{Minor leaks in the OSS are acceptable. Due to the cold temperature of liquid oxygen}
  \noteItem{Leaks are considered as minor if only gaseous oxygen exits the system. As soon as liquid oxygen leaves the system the leak is considered as major.}
  \noteItem{}
  \noteItem{}
  \noteItem{}
  \noteItem{}
  \noteItem{}
  \noteItem{}
  \noteItem{}
  \noteItem{}
  \noteItem{}
  \noteItem{}
  \noteItem{}
  \noteItem{}
  \noteItem{}
  \noteItem{}
  \noteItem{}
  \noteItem{}
  \noteItem{}
  \noteItem{}
  \noteItem{}
  \noteItem{.}

\end{tabularx}
